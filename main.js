const gameBoard = (() => {
  const board = Array.from(document.querySelector("#gameBoard").children);
  console.log(board);
})();

const displayController = (() => {})();

const playerFactory = () => {
  return {};
};
